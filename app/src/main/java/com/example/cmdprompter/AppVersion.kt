package com.example.cmdprompter

/**
 * 版本号：三段式 主版本.次版本.修订号
 * 修改此处后，请同步 app/build.gradle.kts 的 versionName / versionCode，
 * 以及 .github/workflows/build-apk.yml 的 VERSION 环境变量。
 */
object AppVersion {
    const val NAME = "1.3.0"
    const val CODE = 6
}
